# VIB Results Analysis and Final Delivery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the completed MNIST/Fashion-MNIST VIB experiment artifacts into report-ready figures, a stronger Chinese dashboard, and a clean final project delivery.

**Architecture:** Keep offline experiment artifacts as the source of truth. Add a small analysis/export layer that reads `artifacts/full`, writes derived CSV/Markdown/SVG/PNG outputs under `report/figures` and `report/results`, then update the React dashboard and Chinese report to consume or quote only real artifact values.

**Tech Stack:** Python 3.10+, matplotlib, numpy, existing PyTorch artifacts, FastAPI, React/Vite/Recharts, Markdown.

---

## File Structure

```text
vib-robustness-lab/
├── artifacts/full/                         # completed 14-experiment artifact set
├── scripts/
│   ├── summarize_results.py                # new: derive tables and figure files from artifacts/full
│   └── validate_full_artifacts.py           # new: reusable schema/completeness check
├── report/
│   ├── outline.md                          # existing outline, keep as outline
│   ├── results/
│   │   ├── metrics_summary.csv             # generated by summarize_results.py
│   │   ├── robustness_summary.csv          # generated by summarize_results.py
│   │   └── key_findings.md                 # generated/reviewed, then used by report
│   ├── figures/
│   │   ├── mnist_beta_accuracy.svg
│   │   ├── fashion_mnist_beta_accuracy.svg
│   │   ├── mnist_beta_kl.svg
│   │   ├── fashion_mnist_beta_kl.svg
│   │   ├── mnist_robustness.svg
│   │   ├── fashion_mnist_robustness.svg
│   │   └── information_plane.svg
│   └── final_report.md                     # new Chinese report draft using real results
├── frontend/src/
│   ├── App.tsx                             # modify: add dataset filter and interpretation panel
│   ├── components/
│   │   ├── DatasetTabs.tsx                 # new: dataset-level filtering
│   │   └── InterpretationPanel.tsx         # new: Chinese explanation for selected experiment
│   └── lib/metrics.ts                      # modify: dataset filter and interpretation helpers
└── tests/
    ├── test_full_artifact_validation.py    # new: artifact completeness tests
    └── test_result_summary.py              # new: summary derivation tests with tiny temp artifacts
```

Boundaries:

- `artifacts/full` remains generated data and should not be hand-edited.
- `scripts/summarize_results.py` reads artifacts and writes report products.
- `report/final_report.md` may be edited manually, but every numeric claim must come from `report/results/*.csv` or `artifacts/full`.
- Frontend should stay artifact-driven; do not add online training.

---

## Task 1: Full Artifact Validation Script

**Files:**
- Create: `scripts/validate_full_artifacts.py`
- Create: `tests/test_full_artifact_validation.py`

- [ ] **Step 1: Write failing validation tests**

Create `tests/test_full_artifact_validation.py`:

```python
import json
from pathlib import Path

from scripts.validate_full_artifacts import validate_artifact_root


def _write_json(path: Path, payload: dict):
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(payload), encoding="utf-8")


def test_validate_artifact_root_accepts_complete_experiment(tmp_path: Path):
    _write_json(
        tmp_path / "index.json",
        {
            "datasets": ["mnist"],
            "experiments": [
                {"id": "mnist_vib_beta_0_001", "dataset": "mnist", "model": "vib", "beta": 0.001, "path": "mnist_vib_beta_0_001"}
            ],
        },
    )
    exp = tmp_path / "mnist_vib_beta_0_001"
    _write_json(exp / "metrics.json", {"dataset": "mnist", "model": "vib", "beta": 0.001, "test_accuracy": 0.9, "average_kl": 2.0, "noise_accuracy": {"0.0": 0.9}})
    _write_json(exp / "curves.json", {"noise": [{"sigma": 0.0, "accuracy": 0.9}]})
    _write_json(exp / "latent_pca.json", {"points": [{"x": 0.0, "y": 0.0, "label": 1}]})
    _write_json(exp / "confusion_matrix.json", {"matrix": [[1]]})

    report = validate_artifact_root(tmp_path)

    assert report["experiment_count"] == 1
    assert report["missing"] == []


def test_validate_artifact_root_reports_missing_files(tmp_path: Path):
    _write_json(
        tmp_path / "index.json",
        {
            "datasets": ["mnist"],
            "experiments": [
                {"id": "mnist_vib_beta_0_001", "dataset": "mnist", "model": "vib", "beta": 0.001, "path": "mnist_vib_beta_0_001"}
            ],
        },
    )
    _write_json(tmp_path / "mnist_vib_beta_0_001" / "metrics.json", {"test_accuracy": 0.9})

    report = validate_artifact_root(tmp_path)

    assert "mnist_vib_beta_0_001/curves.json" in report["missing"]
    assert "mnist_vib_beta_0_001/latent_pca.json" in report["missing"]
    assert "mnist_vib_beta_0_001/confusion_matrix.json" in report["missing"]
```

- [ ] **Step 2: Run tests and verify failure**

Run:

```bash
python -m pytest tests/test_full_artifact_validation.py -v
```

Expected: FAIL because `scripts.validate_full_artifacts` does not exist.

- [ ] **Step 3: Implement validation script**

Create `scripts/validate_full_artifacts.py`:

```python
import argparse
import json
from pathlib import Path
from typing import Any

REQUIRED_FILES = ["metrics.json", "curves.json", "latent_pca.json", "confusion_matrix.json"]


def _read_json(path: Path) -> Any:
    return json.loads(path.read_text(encoding="utf-8"))


def validate_artifact_root(root: Path) -> dict[str, Any]:
    index = _read_json(root / "index.json")
    missing: list[str] = []
    for experiment in index["experiments"]:
        experiment_dir = root / experiment["path"]
        for filename in REQUIRED_FILES:
            if not (experiment_dir / filename).exists():
                missing.append(f"{experiment['id']}/{filename}")
    return {
        "datasets": index["datasets"],
        "experiment_count": len(index["experiments"]),
        "missing": missing,
    }


def main() -> None:
    parser = argparse.ArgumentParser(description="Validate VIB experiment artifacts.")
    parser.add_argument("root", type=Path, nargs="?", default=Path("artifacts/full"))
    args = parser.parse_args()
    report = validate_artifact_root(args.root)
    print(json.dumps(report, ensure_ascii=False, indent=2))
    if report["missing"]:
        raise SystemExit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run tests and real validation**

Run:

```bash
python -m pytest tests/test_full_artifact_validation.py -v
python scripts/validate_full_artifacts.py artifacts/full
```

Expected: tests pass; real validation reports `experiment_count: 14` and `missing: []`.

---

## Task 2: Result Summary Exporter

**Files:**
- Create: `scripts/summarize_results.py`
- Create: `tests/test_result_summary.py`
- Create generated: `report/results/metrics_summary.csv`
- Create generated: `report/results/robustness_summary.csv`
- Create generated: `report/results/key_findings.md`

- [ ] **Step 1: Write failing summary tests**

Create `tests/test_result_summary.py`:

```python
import json
from pathlib import Path

from scripts.summarize_results import build_metric_rows, build_robustness_rows, derive_key_findings


def _write_json(path: Path, payload: dict):
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(payload), encoding="utf-8")


def _make_artifacts(root: Path):
    _write_json(
        root / "index.json",
        {
            "datasets": ["mnist"],
            "experiments": [
                {"id": "mnist_cnn_baseline", "dataset": "mnist", "model": "cnn", "beta": 0.0, "path": "mnist_cnn_baseline"},
                {"id": "mnist_vib_beta_0_01", "dataset": "mnist", "model": "vib", "beta": 0.01, "path": "mnist_vib_beta_0_01"},
            ],
        },
    )
    _write_json(root / "mnist_cnn_baseline" / "metrics.json", {"dataset": "mnist", "model": "cnn", "beta": 0.0, "test_accuracy": 0.88, "average_kl": 0.0, "train_test_gap": 0.02, "noise_accuracy": {"0.0": 0.88, "0.4": 0.50}})
    _write_json(root / "mnist_vib_beta_0_01" / "metrics.json", {"dataset": "mnist", "model": "vib", "beta": 0.01, "test_accuracy": 0.91, "average_kl": 3.0, "train_test_gap": 0.01, "noise_accuracy": {"0.0": 0.91, "0.4": 0.62}})


def test_build_metric_rows(tmp_path: Path):
    _make_artifacts(tmp_path)

    rows = build_metric_rows(tmp_path)

    assert rows[0]["experiment_id"] == "mnist_cnn_baseline"
    assert rows[1]["beta"] == 0.01
    assert rows[1]["test_accuracy"] == 0.91


def test_build_robustness_rows(tmp_path: Path):
    _make_artifacts(tmp_path)

    rows = build_robustness_rows(tmp_path)

    assert {row["sigma"] for row in rows} == {0.0, 0.4}
    assert any(row["experiment_id"] == "mnist_vib_beta_0_01" and row["accuracy"] == 0.62 for row in rows)


def test_derive_key_findings_identifies_best_accuracy(tmp_path: Path):
    _make_artifacts(tmp_path)

    findings = derive_key_findings(build_metric_rows(tmp_path))

    assert "mnist" in findings
    assert findings["mnist"]["best_clean_experiment"] == "mnist_vib_beta_0_01"
```

- [ ] **Step 2: Run tests and verify failure**

Run:

```bash
python -m pytest tests/test_result_summary.py -v
```

Expected: FAIL because `scripts.summarize_results` does not exist.

- [ ] **Step 3: Implement summary exporter**

Create `scripts/summarize_results.py`:

```python
import argparse
import csv
import json
from collections import defaultdict
from pathlib import Path
from typing import Any


def _read_json(path: Path) -> Any:
    return json.loads(path.read_text(encoding="utf-8"))


def build_metric_rows(root: Path) -> list[dict[str, Any]]:
    index = _read_json(root / "index.json")
    rows: list[dict[str, Any]] = []
    for experiment in index["experiments"]:
        metrics = _read_json(root / experiment["path"] / "metrics.json")
        rows.append(
            {
                "experiment_id": experiment["id"],
                "dataset": experiment["dataset"],
                "model": experiment["model"],
                "beta": float(experiment["beta"]),
                "test_accuracy": float(metrics["test_accuracy"]),
                "train_test_gap": float(metrics["train_test_gap"]),
                "average_kl": float(metrics["average_kl"]),
            }
        )
    return rows


def build_robustness_rows(root: Path) -> list[dict[str, Any]]:
    index = _read_json(root / "index.json")
    rows: list[dict[str, Any]] = []
    for experiment in index["experiments"]:
        metrics = _read_json(root / experiment["path"] / "metrics.json")
        for sigma, accuracy in metrics["noise_accuracy"].items():
            rows.append(
                {
                    "experiment_id": experiment["id"],
                    "dataset": experiment["dataset"],
                    "model": experiment["model"],
                    "beta": float(experiment["beta"]),
                    "sigma": float(sigma),
                    "accuracy": float(accuracy),
                }
            )
    return rows


def derive_key_findings(metric_rows: list[dict[str, Any]]) -> dict[str, dict[str, Any]]:
    grouped: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in metric_rows:
        grouped[row["dataset"]].append(row)
    findings: dict[str, dict[str, Any]] = {}
    for dataset, rows in grouped.items():
        best_clean = max(rows, key=lambda row: row["test_accuracy"])
        best_vib = max([row for row in rows if row["model"] == "vib"], key=lambda row: row["test_accuracy"])
        lowest_kl_vib = min([row for row in rows if row["model"] == "vib"], key=lambda row: row["average_kl"])
        findings[dataset] = {
            "best_clean_experiment": best_clean["experiment_id"],
            "best_clean_accuracy": best_clean["test_accuracy"],
            "best_vib_experiment": best_vib["experiment_id"],
            "best_vib_beta": best_vib["beta"],
            "lowest_kl_vib_experiment": lowest_kl_vib["experiment_id"],
            "lowest_kl_vib_beta": lowest_kl_vib["beta"],
        }
    return findings


def _write_csv(path: Path, rows: list[dict[str, Any]]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    if not rows:
        path.write_text("", encoding="utf-8")
        return
    with path.open("w", newline="", encoding="utf-8") as handle:
        writer = csv.DictWriter(handle, fieldnames=list(rows[0].keys()))
        writer.writeheader()
        writer.writerows(rows)


def _write_findings(path: Path, findings: dict[str, dict[str, Any]]) -> None:
    lines = ["# 实验关键发现", ""]
    for dataset, item in findings.items():
        lines.extend(
            [
                f"## {dataset}",
                "",
                f"- 最高 clean accuracy：`{item['best_clean_experiment']}`，准确率 `{item['best_clean_accuracy']:.4f}`。",
                f"- 最优 VIB β：`{item['best_vib_beta']}`，实验 `{item['best_vib_experiment']}`。",
                f"- 最低 KL 的 VIB 实验：`{item['lowest_kl_vib_experiment']}`，β `{item['lowest_kl_vib_beta']}`。",
                "",
            ]
        )
    path.write_text("\n".join(lines), encoding="utf-8")


def main() -> None:
    parser = argparse.ArgumentParser(description="Summarize VIB full experiment artifacts.")
    parser.add_argument("--artifact-root", type=Path, default=Path("artifacts/full"))
    parser.add_argument("--output-dir", type=Path, default=Path("report/results"))
    args = parser.parse_args()
    metric_rows = build_metric_rows(args.artifact_root)
    robustness_rows = build_robustness_rows(args.artifact_root)
    findings = derive_key_findings(metric_rows)
    _write_csv(args.output_dir / "metrics_summary.csv", metric_rows)
    _write_csv(args.output_dir / "robustness_summary.csv", robustness_rows)
    _write_findings(args.output_dir / "key_findings.md", findings)
    print(f"Wrote {len(metric_rows)} metric rows and {len(robustness_rows)} robustness rows to {args.output_dir}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run tests and exporter**

Run:

```bash
python -m pytest tests/test_result_summary.py -v
python scripts/summarize_results.py --artifact-root artifacts/full --output-dir report/results
```

Expected: tests pass; exporter writes 14 metric rows and 70 robustness rows.

---

## Task 3: Report Figure Exporter

**Files:**
- Modify: `scripts/summarize_results.py`
- Create generated SVGs under `report/figures/`

- [ ] **Step 1: Add plotting functions**

Add these imports to `scripts/summarize_results.py`:

```python
import matplotlib.pyplot as plt
```

Add these functions before `main()`:

```python
def _dataset_rows(rows: list[dict[str, Any]], dataset: str, model: str | None = None) -> list[dict[str, Any]]:
    selected = [row for row in rows if row["dataset"] == dataset]
    if model is not None:
        selected = [row for row in selected if row["model"] == model]
    return sorted(selected, key=lambda row: row["beta"])


def write_figures(metric_rows: list[dict[str, Any]], robustness_rows: list[dict[str, Any]], figure_dir: Path) -> None:
    figure_dir.mkdir(parents=True, exist_ok=True)
    for dataset in sorted({row["dataset"] for row in metric_rows}):
        vib_rows = _dataset_rows(metric_rows, dataset, "vib")
        betas = [row["beta"] for row in vib_rows]
        accuracy = [row["test_accuracy"] for row in vib_rows]
        kl = [row["average_kl"] for row in vib_rows]

        plt.figure(figsize=(6, 4))
        plt.plot(betas, accuracy, marker="o")
        plt.xscale("symlog", linthresh=1e-4)
        plt.xlabel("beta")
        plt.ylabel("clean accuracy")
        plt.title(f"{dataset}: beta vs clean accuracy")
        plt.tight_layout()
        plt.savefig(figure_dir / f"{dataset}_beta_accuracy.svg")
        plt.close()

        plt.figure(figsize=(6, 4))
        plt.plot(betas, kl, marker="o", color="#0f8f7d")
        plt.xscale("symlog", linthresh=1e-4)
        plt.xlabel("beta")
        plt.ylabel("average KL proxy")
        plt.title(f"{dataset}: beta vs KL proxy")
        plt.tight_layout()
        plt.savefig(figure_dir / f"{dataset}_beta_kl.svg")
        plt.close()

        plt.figure(figsize=(6, 4))
        for beta in betas:
            rows_for_beta = [row for row in robustness_rows if row["dataset"] == dataset and row["model"] == "vib" and row["beta"] == beta]
            rows_for_beta = sorted(rows_for_beta, key=lambda row: row["sigma"])
            plt.plot([row["sigma"] for row in rows_for_beta], [row["accuracy"] for row in rows_for_beta], marker="o", label=f"β={beta:g}")
        plt.xlabel("Gaussian noise sigma")
        plt.ylabel("accuracy")
        plt.title(f"{dataset}: robustness curves")
        plt.legend(fontsize=8)
        plt.tight_layout()
        plt.savefig(figure_dir / f"{dataset}_robustness.svg")
        plt.close()

    plt.figure(figsize=(6, 4))
    for dataset in sorted({row["dataset"] for row in metric_rows}):
        rows = _dataset_rows(metric_rows, dataset, "vib")
        plt.scatter([row["average_kl"] for row in rows], [row["test_accuracy"] for row in rows], label=dataset)
    plt.xlabel("average KL proxy")
    plt.ylabel("clean accuracy")
    plt.title("Information plane")
    plt.legend()
    plt.tight_layout()
    plt.savefig(figure_dir / "information_plane.svg")
    plt.close()
```

- [ ] **Step 2: Wire figure export into main**

In `main()`, after `_write_findings(...)`, add:

```python
    write_figures(metric_rows, robustness_rows, Path("report/figures"))
```

- [ ] **Step 3: Install/verify matplotlib and run exporter**

Run:

```bash
python -c "import matplotlib; print(matplotlib.__version__)"
python scripts/summarize_results.py --artifact-root artifacts/full --output-dir report/results
```

Expected: command succeeds and writes the seven SVG files listed in File Structure.

- [ ] **Step 4: Verify generated figures exist**

Run:

```bash
python - <<'PY'
from pathlib import Path
expected = [
  'mnist_beta_accuracy.svg', 'fashion_mnist_beta_accuracy.svg',
  'mnist_beta_kl.svg', 'fashion_mnist_beta_kl.svg',
  'mnist_robustness.svg', 'fashion_mnist_robustness.svg',
  'information_plane.svg',
]
missing = [name for name in expected if not (Path('report/figures') / name).exists()]
print({'missing': missing})
raise SystemExit(1 if missing else 0)
PY
```

Expected: `{'missing': []}`.

---

## Task 4: Dashboard Dataset Filter and Interpretation Panel

**Files:**
- Create: `frontend/src/components/DatasetTabs.tsx`
- Create: `frontend/src/components/InterpretationPanel.tsx`
- Modify: `frontend/src/lib/metrics.ts`
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/style.css`

- [ ] **Step 1: Add frontend helpers**

Modify `frontend/src/lib/metrics.ts` by adding:

```ts
export function datasetLabel(dataset: string): string {
  if (dataset === 'fashion_mnist') return 'Fashion-MNIST'
  if (dataset === 'mnist') return 'MNIST'
  return dataset
}

export function modelLabel(model: string): string {
  return model === 'cnn' ? '普通 CNN' : 'VIB-CNN'
}

export function compressionInterpretation(beta: number, kl: number): string {
  if (beta >= 1 || kl < 0.1) return '压缩非常强，表示几乎被推向先验，可能已经损失任务信息。'
  if (beta >= 0.1) return '压缩较强，KL proxy 明显下降，适合观察鲁棒性与精度的折中。'
  if (beta > 0) return '压缩适中，通常能保留分类信息，同时减少输入细节依赖。'
  return '未施加 KL 惩罚，表示会保留更多输入信息，也可能保留噪声。'
}
```

- [ ] **Step 2: Create dataset tabs**

Create `frontend/src/components/DatasetTabs.tsx`:

```tsx
import { datasetLabel } from '../lib/metrics'

export function DatasetTabs({
  datasets,
  selectedDataset,
  onSelect,
}: {
  datasets: string[]
  selectedDataset: string
  onSelect: (dataset: string) => void
}) {
  return (
    <div className="dataset-tabs" aria-label="数据集筛选">
      {datasets.map((dataset) => (
        <button
          key={dataset}
          className={dataset === selectedDataset ? 'active' : ''}
          type="button"
          onClick={() => onSelect(dataset)}
        >
          {datasetLabel(dataset)}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 3: Create interpretation panel**

Create `frontend/src/components/InterpretationPanel.tsx`:

```tsx
import { compressionInterpretation, datasetLabel, modelLabel } from '../lib/metrics'
import type { ExperimentPayload } from '../types'

export function InterpretationPanel({ experiment }: { experiment: ExperimentPayload }) {
  const metrics = experiment.metrics
  return (
    <section className="interpretation-panel">
      <p className="section-kicker">实验解读</p>
      <h2>{datasetLabel(metrics.dataset)} · {modelLabel(metrics.model)}</h2>
      <p>{compressionInterpretation(metrics.beta, metrics.average_kl)}</p>
      <dl>
        <div>
          <dt>β</dt>
          <dd>{metrics.beta}</dd>
        </div>
        <div>
          <dt>KL proxy</dt>
          <dd>{metrics.average_kl.toFixed(4)}</dd>
        </div>
        <div>
          <dt>σ=0.4 准确率</dt>
          <dd>{((metrics.noise_accuracy['0.4'] ?? 0) * 100).toFixed(2)}%</dd>
        </div>
      </dl>
    </section>
  )
}
```

- [ ] **Step 4: Wire into App**

Modify `frontend/src/App.tsx`:

- Import `DatasetTabs` and `InterpretationPanel`.
- Add state:

```ts
const [selectedDataset, setSelectedDataset] = useState<string>('')
```

- After sorting experiments in the first `useEffect`, set:

```ts
const firstDataset = payload.datasets[0] ?? ''
const filtered = sorted.filter((experiment) => experiment.dataset === firstDataset)
const firstId = filtered[0]?.id ?? sorted[0]?.id ?? ''
setSelectedDataset(firstDataset)
```

- Add a second effect that changes `selectedId` when `selectedDataset` changes:

```ts
useEffect(() => {
  if (!index || !selectedDataset) return
  const next = index.experiments.find((experiment) => experiment.dataset === selectedDataset)
  if (next && next.id !== selectedId) setSelectedId(next.id)
}, [index, selectedDataset, selectedId])
```

- Compute filtered experiments:

```ts
const visibleExperiments = index.experiments.filter((experiment) => experiment.dataset === selectedDataset)
```

- Render tabs above `ExperimentSelector`:

```tsx
<DatasetTabs datasets={index.datasets} selectedDataset={selectedDataset} onSelect={setSelectedDataset} />
<ExperimentSelector experiments={visibleExperiments} selectedId={selectedId} onSelect={setSelectedId} />
```

- Render interpretation panel after `MetricCards`:

```tsx
<InterpretationPanel experiment={selected} />
```

- [ ] **Step 5: Add CSS**

Append to `frontend/src/style.css`:

```css
.dataset-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-bottom: 18px;
}

.dataset-tabs button {
  padding: 10px 16px;
  border: 1px solid rgba(22, 33, 30, 0.18);
  border-radius: 999px;
  background: rgba(255, 252, 244, 0.72);
  color: #4e5b55;
  cursor: pointer;
  font-weight: 700;
}

.dataset-tabs button.active {
  border-color: #0f8f7d;
  background: #0f8f7d;
  color: #fffaf0;
}

.interpretation-panel {
  margin-top: 22px;
  padding: 26px 0 6px;
  border-top: 1px solid rgba(22, 33, 30, 0.14);
}

.interpretation-panel h2 {
  margin: 0;
  font-size: 30px;
  letter-spacing: -0.04em;
}

.interpretation-panel p:not(.section-kicker) {
  max-width: 760px;
  color: #4e5b55;
  line-height: 1.8;
}

.interpretation-panel dl {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1px;
  overflow: hidden;
  border: 1px solid rgba(22, 33, 30, 0.12);
  border-radius: 18px;
  background: rgba(22, 33, 30, 0.1);
}

.interpretation-panel dl div {
  padding: 18px;
  background: rgba(255, 252, 244, 0.72);
}

.interpretation-panel dt {
  color: #64746f;
  font-size: 13px;
}

.interpretation-panel dd {
  margin: 8px 0 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 28px;
}

@media (max-width: 900px) {
  .interpretation-panel dl {
    grid-template-columns: 1fr;
  }
}
```

- [ ] **Step 6: Build and browser verify**

Run:

```bash
cd frontend && npm run build
```

Then start backend with `VIB_ARTIFACT_DIR=artifacts/full`, start frontend dev server, and verify in browser:

- Dataset tabs show MNIST and Fashion-MNIST.
- Selecting Fashion-MNIST filters the experiment selector.
- Interpretation panel text changes for β=1 versus β=0.01.

---

## Task 5: Chinese Final Report Draft

**Files:**
- Create: `report/final_report.md`
- Read: `report/results/key_findings.md`
- Read: `report/results/metrics_summary.csv`

- [ ] **Step 1: Generate source summaries first**

Run:

```bash
python scripts/summarize_results.py --artifact-root artifacts/full --output-dir report/results
```

Expected: `report/results/key_findings.md` exists and contains MNIST/Fashion-MNIST findings.

- [ ] **Step 2: Create final report draft**

Create `report/final_report.md` with this structure and use real numbers from `report/results/metrics_summary.csv`:

```markdown
# 基于变分信息瓶颈的神经网络表示压缩与鲁棒泛化分析

## 摘要

本文复现变分信息瓶颈（Variational Information Bottleneck, VIB）在图像分类表示学习中的核心思想，并在 MNIST 与 Fashion-MNIST 上比较普通 CNN 与 VIB-CNN。实验使用 KL(q(z|x)||p(z)) 作为 I(X;Z) 的变分上界 proxy，分析 β 对表示压缩、clean accuracy 和 Gaussian noise 鲁棒性的影响。结果显示，适中的 β 能在保持分类性能的同时显著降低 KL proxy；当 β 过大时，表示被过度压缩，任务信息明显丢失。

## 1 引言

信息论中的压缩与预测之间存在天然张力。深度神经网络的中间表示 Z 可以被理解为输入 X 的编码，同时又要保留足够关于标签 Y 的信息。本文研究的问题是：在图像分类任务中，是否可以通过控制表示压缩强度 β，使模型减少对输入细节或噪声的依赖，从而获得更好的鲁棒泛化。

## 2 信息论背景

信息瓶颈目标可写为：

```math
\max I(Z;Y) - \beta I(Z;X)
```

其中 I(Z;Y) 表示任务相关信息，I(Z;X) 表示表示中保留的输入信息。VIB 使用变分分布 q_phi(z|x) 和先验 p(z)，将 I(Z;X) 近似为可优化的 KL 上界。本文所有图表中的 KL 都是 KL proxy / variational upper bound，不是精确互信息估计。

## 3 方法

普通 CNN 使用卷积编码器得到 latent 表示并直接分类。VIB-CNN 使用同样的卷积编码器，但输出 mu 与 logvar，通过重参数化采样 z，并用 CE + beta * KL 训练。

## 4 实验设置

数据集为 MNIST 与 Fashion-MNIST。每个数据集包含一个 CNN baseline，以及 β ∈ {0, 1e-4, 1e-3, 1e-2, 1e-1, 1} 的 VIB-CNN。鲁棒性测试使用 Gaussian noise，σ ∈ {0.0, 0.1, 0.2, 0.3, 0.4}。

## 5 结果与分析

### 5.1 MNIST

填写 MNIST 的 CNN baseline、最优 VIB、β=1 过压缩结果。引用 `report/results/metrics_summary.csv` 中真实数值。

### 5.2 Fashion-MNIST

填写 Fashion-MNIST 的 CNN baseline、最优 VIB、β=1 过压缩结果。引用 `report/results/metrics_summary.csv` 中真实数值。

### 5.3 信息平面

结合 `report/figures/information_plane.svg` 说明 KL proxy 与 accuracy 的关系。重点说明：β 增大通常降低 KL；适中 β 能维持 accuracy；β=1 会让 KL 接近 0 并损害 accuracy。

### 5.4 鲁棒性

结合 `report/figures/mnist_robustness.svg` 与 `report/figures/fashion_mnist_robustness.svg`，分析不同 β 在 σ=0.4 时的准确率差异。

## 6 Web 交互系统

Web demo 使用 FastAPI 读取 artifacts/full，并由 React + Recharts 展示实验选择、指标卡、信息平面、鲁棒性曲线和 latent PCA。网页不在线训练，保证部署轻量且结果可复现。

## 7 局限性

本文没有估计精确互信息，只展示 KL proxy。模型结构较小，主要用于课程实验与可解释展示；CIFAR-10、更多 corruption 类型和 adaptive β 可作为后续扩展。

## 8 结论

VIB 将表示学习中的压缩和预测折中显式化。实验表明，β 能有效控制 KL proxy；适中 β 在 MNIST 和 Fashion-MNIST 上保持较好分类性能，而过大 β 会造成任务信息损失。这一结果为从信息论视角理解深度表示的鲁棒泛化提供了直观实验支持。
```

- [ ] **Step 3: Replace placeholder analysis with real values**

Open `report/results/metrics_summary.csv`, fill sections 5.1 and 5.2 with real values. Include at minimum:

- CNN baseline clean accuracy.
- Best VIB clean accuracy and β.
- β=1 clean accuracy and KL.
- One sentence explaining whether moderate β improves clean accuracy over CNN.

- [ ] **Step 4: Verify no placeholders remain**

Run:

```bash
python - <<'PY'
from pathlib import Path
text = Path('report/final_report.md').read_text(encoding='utf-8')
bad = [word for word in ['填写', 'TODO', 'TBD'] if word in text]
print({'placeholders': bad})
raise SystemExit(1 if bad else 0)
PY
```

Expected: `{'placeholders': []}`.

---

## Task 6: Final Verification and Commit Preparation

**Files:**
- Modify only if verification reveals defects.

- [ ] **Step 1: Run Python tests**

Run:

```bash
python -m pytest -v
```

Expected: all tests pass.

- [ ] **Step 2: Validate full artifacts**

Run:

```bash
python scripts/validate_full_artifacts.py artifacts/full
python scripts/summarize_results.py --artifact-root artifacts/full --output-dir report/results
```

Expected: validation reports 14 experiments and no missing files; summary command completes.

- [ ] **Step 3: Build frontend**

Run:

```bash
cd frontend && npm run build
```

Expected: build succeeds. A Vite/Recharts chunk-size warning is acceptable.

- [ ] **Step 4: Browser verify full dashboard**

Run backend and frontend:

```bash
VIB_ARTIFACT_DIR=artifacts/full uvicorn backend.app:app --host 127.0.0.1 --port 8000
cd frontend && npm run dev -- --host 127.0.0.1 --port 5177
```

Verify in browser:

- 14 experiment options are available, or dataset tabs filter them correctly if Task 4 is implemented.
- MNIST and Fashion-MNIST both display.
- Selecting `fashion_mnist_vib_beta_1` displays test accuracy around `60.14%` and KL around `0.021`.

- [ ] **Step 5: Inspect git status**

Run:

```bash
git status --short
```

Expected: source files, sample/full artifacts, report outputs, and logs are visible. Generated directories ignored by `.gitignore` should not appear: `.claude/`, `data/`, `frontend/node_modules/`, `frontend/dist/`, `artifacts/dev/`.

- [ ] **Step 6: Decide commit policy with user**

Do not commit unless the user explicitly asks. If asked to commit, stage specific files only; avoid `git add -A` unless manually reviewed.

---

## Self-Review

Spec coverage:

- Completed experiment artifacts are validated by Task 1.
- Real numeric summaries for report are generated by Task 2.
- Report-ready figures are generated by Task 3.
- Chinese dashboard usability is improved by Task 4.
- Chinese final report draft is covered by Task 5.
- End-to-end verification and commit readiness are covered by Task 6.

Placeholder scan:

- No `TODO` or `TBD` markers are used as implementation instructions.
- Task 5 intentionally includes temporary report text with explicit replacement step and a verification command that fails if placeholders remain.

Type consistency:

- Summary rows use `experiment_id`, `dataset`, `model`, `beta`, `test_accuracy`, `train_test_gap`, and `average_kl` consistently.
- Frontend helper names `datasetLabel`, `modelLabel`, and `compressionInterpretation` are used consistently in component tasks.
- Artifact validation expects the existing generated files: `metrics.json`, `curves.json`, `latent_pca.json`, and `confusion_matrix.json`.
